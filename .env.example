# Optional. Only needed for the Google Drive backup feature — see the
# "Optional: Google Drive backup" section in README.md for how to get one.
# Without it, the app works exactly the same minus that one feature.
VITE_GOOGLE_CLIENT_ID=
